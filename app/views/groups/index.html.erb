<h1>Your Groups

  <a href="<%= new_group_path %>" class="btn btn-info pull-right"><i class="fa fa-plus"></i> New Group <i class="fa fa-users"></i></a>
</h1>

<ul>
  <% if @groups.each do |g| %>
    <li><%= link_to g.name, group_path(g) %></li>
  <% end.empty? %>
    <p>You do not belong to any groups yet.</p>
  <% end %>
</ul>


<h1>Public Groups</h1>

<ul>
  <% if @public_groups.each do |g| %>
    <li><%= link_to g.name, group_path(g) %></li>
  <% end.empty? %>
  <p>There are no public groups.</p>
  <% end %>
</ul>
